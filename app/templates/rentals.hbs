{{outlet}}
<div class="jumbo">
  <div class="right tomster"></div>
  <h2>Welcome, B.</h2>
  <p>
    I really hope you find stuff.
  </p>
  {{#link-to 'about' class="button"}}
  About Us
  {{/link-to}}
</div>

{{#list-filter
   filter=(action 'filterByCity')
   as |rentals|}}
  <ul class="results">
    {{#each rentals as |rentalUnit|}}
      <li>{{rental-listing rental=rentalUnit}}</li>
    {{/each}}
  </ul>
{{/list-filter}}
